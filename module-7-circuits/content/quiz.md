# Electronics Quiz

## Question 1
In a standard voltage divider circuit with two resistors, $R_1$ (connected to the source) and $R_2$ (connected to ground), what is the formula to calculate the output voltage $V_{out}$?

- [x] $V_{out} = V_{in} \cdot \frac{R_2}{R_1 + R_2}$
- [ ] $V_{out} = V_{in} \cdot \frac{R_1}{R_2}$
- [ ] $V_{out} = V_{in} \cdot \frac{R_1}{R_1 + R_2}$
- [ ] $V_{out} = V_{in} \cdot (R_1 + R_2)$

**Hint:** Consider which resistor the output voltage is actually measured across relative to the ground.

## Question 2
What is the primary purpose of using a pull-up resistor with an Arduino digital input pin and a momentary push button?

- [x] To ensure the pin stays at a stable 'High' state when the button is not pressed.
- [ ] To increase the speed at which the Arduino can process the button press signal.
- [ ] To protect the Arduino pin from a short circuit when the button is pressed.
- [ ] To convert the analog signal of the button into a digital signal.

**Hint:** Think about what happens to the electrical signal when the circuit is 'open' and nothing is connecting the pin to a known voltage.

## Question 3
If a digital input pin on an Arduino is 'floating,' what behavior should a programmer expect?

- [x] The pin may randomly toggle between 'High' and 'Low' due to environmental electrical noise.
- [ ] The pin will default to a constant 'Low' state because there is no power source connected.
- [ ] The Arduino will automatically trigger a software error to prevent damage to the chip.
- [ ] The pin will default to a constant 'High' state due to internal capacitance.

**Hint:** Consider how sensitive electronic inputs react to the electromagnetic fields generated by nearby devices or wiring.

## Question 4
When calculating the current-limiting resistor for an LED, why is the 'forward voltage' ($V_f$) of the LED subtracted from the source voltage ($V_s$)?

- [x] To determine the remaining voltage that will actually drop across the resistor.
- [ ] Because the LED acts as a second battery that pushes voltage back toward the source.
- [ ] To account for the internal resistance of the power supply.
- [ ] To ensure the LED operates at its maximum possible brightness without flickering.

**Hint:** Think about how Ohm's Law ($V = IR$) applies specifically to the resistor in a series circuit containing other components.

## Question 5
You are connecting an LED with a forward voltage of $2V$ to a $5V$ Arduino pin. If you want a current of $20mA$ ($0.02A$), which resistor value should you use?

- [x] $150 \Omega$
- [ ] $250 \Omega$
- [ ] $100 \Omega$
- [ ] $350 \Omega$

**Hint:** Use the formula $R = \frac{V_{source} - V_{LED}}{I_{desired}}$ and remember to convert milliamps to amps.

## Question 6
What is the 'time constant' ($\tau$) of an RC circuit, and what does it represent?

- [x] $\tau = R \cdot C$; it is the time required to charge the capacitor to approximately $63\%$ of the supply voltage.
- [ ] $\tau = \frac{R}{C}$; it represents the total time it takes for a capacitor to reach a full charge.
- [ ] $\tau = R \cdot C$; it is the frequency at which the circuit oscillates between high and low.
- [ ] $\tau = V \cdot C$; it determines the maximum voltage a capacitor can safely hold.

**Hint:** The unit for this value is seconds, and it is calculated by multiplying the two primary components of the circuit.

## Question 7
In a pull-down resistor configuration, how should the button and resistor be wired to a digital input pin?

- [x] The resistor connects the pin to Ground ($GND$), and the button connects the pin to $5V$.
- [ ] The resistor connects the pin to $5V$, and the button connects the pin to Ground ($GND$).
- [ ] Both the resistor and the button are connected in series between the pin and $5V$.
- [ ] The resistor is connected in parallel with the button, both leading to the $5V$ rail.

**Hint:** Recall that 'pull-down' means the default state is pulled toward the lowest voltage potential in the circuit.

## Question 8
If you increase the resistance ($R$) in an RC timing circuit while keeping the capacitance ($C$) the same, what happens to the time it takes to charge the capacitor?

- [x] The charging time increases.
- [ ] The charging time decreases.
- [ ] The charging time remains identical, but the final voltage is lower.
- [ ] The capacitor will fail to charge at all.

**Hint:** Think of the resistor as a pipe and the capacitor as a tank; what happens to the fill time if the pipe becomes narrower?

## Question 9
Which component is best suited to create a simple time delay in an electronic circuit without using a microcontroller's code?

- [x] An RC (Resistor-Capacitor) circuit.
- [ ] A Voltage Divider.
- [ ] A single Pull-up Resistor.
- [ ] An LED.

**Hint:** Consider which combination of components relies on the gradual accumulation of energy over time.

## Question 10
In a voltage divider where $R_1 = 10k\Omega$ and $R_2 = 10k\Omega$, what will be the output voltage if the input is $5V$?

- [x] $2.5V$
- [ ] $5V$
- [ ] $0V$
- [ ] $1.25V$

**Hint:** Think about how two identical obstacles in a path would share the total effort required to pass through them.
